---
- hosts: cisco_routers
  gather_facts: no
  connection: network_cli

  tasks:
    - name: show run
      ios_command:
        commands:
          - show run
        hosts: "{{ ansible_host }}"
      register: config
      
    - name: save output to backup folder
      copy:
        content: "{{ config.stdout[0] }}"